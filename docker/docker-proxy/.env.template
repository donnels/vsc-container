# Proxy configuration
PROXY_DOMAIN=vsagcrd.org

# SSL certificate paths (automatically managed by cert-generator)
SSL_CERT_PATH=/etc/nginx/certs/fullchain.pem
SSL_KEY_PATH=/etc/nginx/certs/privkey.pem

# Optional: Custom nginx worker processes
NGINX_WORKER_PROCESSES=auto
