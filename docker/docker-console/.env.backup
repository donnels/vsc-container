# Proxy configuration
PROXY_DOMAIN=dev.vsagcrd.org

# Proxy certificate name (must match certbot configuration)
PROXY_CERT_NAME=proxy

# Optional: Custom nginx worker processes
NGINX_WORKER_PROCESSES=auto
